env:
    node: true
parser: '@babel/eslint-parser'
extends: ['airbnb-base', 'prettier']
rules:
  prettier/prettier: ['error', 
    {'singleQuote': true}]
  no-plusplus: 0
  func-names: 0

plugins:
  - 'prettier'
